export type LicenseRecord = {
  activated: true;
  edition: string;
  licensedTo: string;
  expiresAt: string;
  features: string;
  deviceLimit: number;
  hardwareId: string;
  activatedAt: string;
};
